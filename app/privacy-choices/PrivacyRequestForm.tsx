"use client"
import { FormEvent, useState } from "react"
export function PrivacyRequestForm() { const [message, setMessage] = useState(""); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); const response = await fetch("/api/privacy-requests", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(Object.fromEntries(form)) }); setMessage(response.ok ? "Your request was received for manual review." : "We could not submit the request. Please try again later."); if (response.ok) event.currentTarget.reset() } const field = "mt-1 w-full border border-[#d8cdba] bg-white px-3 py-3 text-sm outline-none focus:border-[#c2683f]"; return <form onSubmit={submit} className="mt-6 grid gap-4" aria-describedby="privacy-request-note"><label>Name<input required name="name" className={field}/></label><label>Email<input required type="email" name="email" className={field}/></label><label>Request type<select name="requestType" className={field}><option value="ACCESS">Access request</option><option value="CORRECTION">Correction request</option><option value="DELETION">Deletion request</option><option value="QUESTION">Privacy question</option><option value="DO_NOT_SELL_OR_SHARE">Do not sell or share request</option></select></label><label>Details (optional)<textarea name="details" className={field} rows={4}/></label><p id="privacy-request-note" className="text-sm text-[#5d6b78]">Requests are reviewed manually. Do not include payment credentials, door codes, or other sensitive details.</p>{message && <p role="status" className="text-sm text-[#5d6b78]">{message}</p>}<button className="w-fit bg-[#28323b] px-5 py-3 text-xs uppercase tracking-[.16em] text-white">Submit request</button></form> }
