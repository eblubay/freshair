"use client"

import { BraintreeCheckout } from "@/app/_components/BraintreeCheckout"
import { StripeCheckout } from "@/app/_components/StripeCheckout"
import { useMemo, useRef, useState } from "react"

type DirectBookingProps = { propertyId: string; maxGuests: number }
type Quote = { quoteId: string; expiresAt: string; nights: number; currency: string; subtotal: number; cleaningFee: number; taxes: number; discount: number; total: number; directBookingEnabled: boolean; paymentProvider: "BRAINTREE" | "STRIPE"; checkinTime: string; timezone: string; fullRefundDays: number; partialRefundStartDays: number; partialRefundPercentage: number; houseRulesVersion: string }
type Hold = { reservationId: string; confirmationCode: string; holdExpiresAt: string }
const money = (value: number, currency: string) => new Intl.NumberFormat("en-US", { style: "currency", currency }).format(value / 100)
const dateText = (date: string, days: number, time: string) => { const value = new Date(`${date}T12:00:00`); value.setDate(value.getDate() - days); return `${value.toLocaleDateString("en-US", { month: "long", day: "numeric" })}, ${new Date(`1970-01-01T${time}`).toLocaleTimeString("en-US", { hour: "numeric", minute: "2-digit" })}` }

export function DirectBooking({ propertyId, maxGuests }: DirectBookingProps) {
	const [checkIn, setCheckIn] = useState(""); const [checkOut, setCheckOut] = useState(""); const [adults, setAdults] = useState("2"); const [children, setChildren] = useState("0"); const [couponCode, setCouponCode] = useState(""); const [referralCode, setReferralCode] = useState(""); const [legalConsent, setLegalConsent] = useState(false); const [houseRulesConsent, setHouseRulesConsent] = useState(false); const [quote, setQuote] = useState<Quote | null>(null); const [hold, setHold] = useState<Hold | null>(null); const [state, setState] = useState<"idle" | "loading" | "error" | "confirmed">("idle"); const [error, setError] = useState<string | null>(null); const [guest, setGuest] = useState({ firstName: "", lastName: "", email: "", phone: "" }); const requestId = useRef<string | null>(null)
	const canQuote = useMemo(() => Boolean(checkIn && checkOut && checkOut > checkIn && Number(adults) + Number(children) <= maxGuests), [adults, checkIn, checkOut, children, maxGuests])
	const input = () => ({ propertyId, checkIn, checkOut, adults: Number(adults), children: Number(children), couponCode: couponCode.trim() || undefined, referralCode: referralCode.trim() || undefined })
	const reset = () => { setQuote(null); setHold(null); setError(null); requestId.current = null }
	async function getQuote() { if (!canQuote) return setError(`Choose valid dates and no more than ${maxGuests} guests.`); setState("loading"); setError(null); try { const response = await fetch("/api/bookings/quote", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(input()) }); const data = await response.json(); if (!response.ok) throw new Error(data.error ?? "Unable to prepare a quote."); setQuote(data); setState("idle") } catch (cause) { setError(cause instanceof Error ? cause.message : "Unable to prepare a quote."); setState("error") } }
	async function holdDates() { if (!quote) return; if (!guest.firstName || !guest.lastName || !guest.email) return setError("Enter your name and email before continuing."); if (!legalConsent || !houseRulesConsent) return setError("You must accept the legal terms and House Rules before payment."); setState("loading"); try { requestId.current ??= crypto.randomUUID(); const response = await fetch("/api/bookings/hold", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ quoteInput: input(), guest, consent: { termsAccepted: true, privacyAcknowledged: true, houseRulesAccepted: true, termsVersion: "2026-08-11", privacyVersion: "2026-08-11", houseRulesVersion: quote.houseRulesVersion }, clientRequestId: requestId.current }) }); const data = await response.json(); if (!response.ok) throw new Error(data.error ?? "Unable to hold dates."); setHold(data); setState("idle") } catch (cause) { setError(cause instanceof Error ? cause.message : "Unable to hold dates."); setState("error") } }
	const field = "w-full border border-[#d8cdba] bg-white px-3 py-3 text-sm text-[#28323b] outline-none focus:border-[#c2683f]"
	if (state === "confirmed") return <div className="border border-[#e6ddcf] bg-[#fdfbf7] p-6 text-center"><p className="text-[11px] uppercase tracking-[.18em] text-[#8d7c66]">Booking confirmed</p><h3 className="mt-2 font-serif text-2xl">Thank you for booking.</h3><p className="mt-3 text-sm text-[#5d6b78]">Your confirmation code is <strong>{hold?.confirmationCode}</strong>.</p></div>
	return <section className="border border-[#e6ddcf] bg-[#fdfbf7] p-6 sm:p-8">
		<p className="text-[11px] uppercase tracking-[.18em] text-[#8d7c66]">Direct booking</p><h3 className="mt-2 font-serif text-2xl">Check dates and pricing</h3>
		<div className="mt-6 grid grid-cols-2 gap-3"><input aria-label="Direct booking check-in" className={field} type="date" min={new Date().toISOString().slice(0, 10)} value={checkIn} onChange={(event) => { setCheckIn(event.target.value); reset() }}/><input aria-label="Direct booking check-out" className={field} type="date" min={checkIn} value={checkOut} onChange={(event) => { setCheckOut(event.target.value); reset() }}/><select aria-label="Adults" className={field} value={adults} onChange={(event) => { setAdults(event.target.value); reset() }}>{Array.from({ length: maxGuests }, (_, i) => <option key={i} value={i + 1}>{i + 1} adult{i ? "s" : ""}</option>)}</select><select aria-label="Children" className={field} value={children} onChange={(event) => { setChildren(event.target.value); reset() }}>{Array.from({ length: maxGuests }, (_, i) => <option key={i} value={i}>{i} children</option>)}</select></div>
		<input aria-label="Coupon code" className={`${field} mt-3`} placeholder="Coupon code (optional)" value={couponCode} onChange={(event) => { setCouponCode(event.target.value); reset() }}/><input aria-label="Referral code" className={`${field} mt-3`} placeholder="Referral code (optional)" value={referralCode} onChange={(event) => { setReferralCode(event.target.value); reset() }}/><button type="button" disabled={state === "loading"} onClick={getQuote} className="mt-4 w-full bg-[#28323b] px-5 py-3 text-xs uppercase tracking-[.16em] text-white disabled:opacity-60">{state === "loading" ? "Checking…" : "Check availability"}</button>{error && <p role="alert" className="mt-4 text-sm text-[#b33939]">{error}</p>}
		{quote && <><div className="mt-6 border-y border-[#e6ddcf] py-4 text-sm text-[#5d6b78]"><p>{quote.nights} nights <span className="float-right">{money(quote.subtotal, quote.currency)}</span></p><p className="mt-2">Cleaning <span className="float-right">{money(quote.cleaningFee, quote.currency)}</span></p><p className="mt-2">Taxes <span className="float-right">{money(quote.taxes, quote.currency)}</span></p><p className="mt-3 font-semibold text-[#28323b]">Total <span className="float-right">{money(quote.total, quote.currency)}</span></p></div>
			{!hold && <div className="mt-5 grid gap-3"><section className="border-l-2 border-[#c2683f] pl-3 text-sm leading-relaxed text-[#5d6b78]"><strong className="text-[#28323b]">Cancellation</strong><p>Full refund of nightly accommodation charges until {dateText(checkIn, quote.fullRefundDays, quote.checkinTime)}.</p><p>{quote.partialRefundPercentage}% nightly-rate refund until {dateText(checkIn, quote.partialRefundStartDays, quote.checkinTime)}.</p><p>After {dateText(checkIn, quote.partialRefundStartDays, quote.checkinTime)}, nightly charges are non-refundable. California 24-hour cancellation rights apply where required by law.</p></section><section className="border-l-2 border-[#c2683f] pl-3 text-sm leading-relaxed text-[#5d6b78]"><strong className="text-[#28323b]">House Rules</strong><p>Check-in after 3:00 PM · Checkout before 10:00 AM · Maximum 4 guests · No pets · Quiet hours 10:00 PM–8:00 AM · No parties, events, commercial photography, or indoor smoking.</p><p className="mt-1">One enclosed-garage space is included; clearance is 74 inches. Verify larger vehicles before booking. Additional cars may require legal off-site/street parking. A cleaning charge of up to $250 may apply for verified indoor smoking; it is not automatically charged.</p></section><div className="grid grid-cols-2 gap-3"><input aria-label="First name" className={field} placeholder="First name" value={guest.firstName} onChange={(event) => setGuest({ ...guest, firstName: event.target.value })}/><input aria-label="Last name" className={field} placeholder="Last name" value={guest.lastName} onChange={(event) => setGuest({ ...guest, lastName: event.target.value })}/></div><input aria-label="Email address" className={field} type="email" placeholder="Email address" value={guest.email} onChange={(event) => setGuest({ ...guest, email: event.target.value })}/><input aria-label="Phone number" className={field} type="tel" placeholder="Phone (optional)" value={guest.phone} onChange={(event) => setGuest({ ...guest, phone: event.target.value })}/><label className="flex items-start gap-3 text-sm text-[#5d6b78]"><input className="mt-1 h-4 w-4 accent-[#c2683f]" type="checkbox" checked={legalConsent} onChange={(event) => setLegalConsent(event.target.checked)}/><span>I agree to the <a className="underline" href="/terms-and-conditions" target="_blank" rel="noreferrer">Terms &amp; Conditions</a> and acknowledge the <a className="underline" href="/privacy-policy" target="_blank" rel="noreferrer">Privacy Policy</a>.</span></label><label className="flex items-start gap-3 text-sm text-[#5d6b78]"><input className="mt-1 h-4 w-4 accent-[#c2683f]" type="checkbox" checked={houseRulesConsent} onChange={(event) => setHouseRulesConsent(event.target.checked)}/><span>I have reviewed and agree to the House Rules.</span></label><button type="button" disabled={state === "loading" || !legalConsent || !houseRulesConsent} onClick={holdDates} className="w-full border border-[#28323b] px-5 py-3 text-xs uppercase tracking-[.16em] text-[#28323b] disabled:opacity-60">Continue to secure payment</button></div>}
			{hold && <><p className="mt-5 text-sm text-[#5d6b78]">Dates are temporarily held until {new Date(hold.holdExpiresAt).toLocaleTimeString([], { hour: "numeric", minute: "2-digit" })}.</p>{quote.paymentProvider === "STRIPE" ? <StripeCheckout reservationId={hold.reservationId} onSuccess={() => setState("confirmed")}/> : <BraintreeCheckout reservationId={hold.reservationId} onSuccess={() => setState("confirmed")}/>}</>}
		</>}
	</section>
}
